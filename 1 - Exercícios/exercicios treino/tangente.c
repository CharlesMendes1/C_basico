#include <stdio.h>

float seno(float angulo);
float cosseno(float angulo);
float tangente(float angulo);
float S,C;
S = seno(angulo);
C = cosseno(angulo);
return s/c ;

int main (){
float a = 30 ;
printf("%f",tangente(a));
return 0 ;

}
